When working with html always start with index.html

What is the difference between an in-line element and block element?
	- block element takes up as much space as it can
	- inline element only takes up as much space as it needs
